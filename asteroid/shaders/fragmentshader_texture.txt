#version 410 core


uniform sampler2DArray texture_array;

in vec2 tex_coord;
in float current_layer;
in float hit;
in float alpha;

out vec4 color;


void main() {
	vec4 current_color= texture(texture_array, vec3(tex_coord.x, tex_coord.y, current_layer));
	if (current_color.a< 0.5) {
		discard;
	}
	
	// hit == 0 : pas touché ; hit == 1 : vient de se faire toucher
	color= mix(vec4(current_color.rgb, alpha), vec4(1.0, 0.0, 0.0, alpha), hit);
}
