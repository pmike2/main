#version 410


out vec4 frag_color;

in vec3 text_coords;

uniform samplerCube skybox;

void main()
{    
    frag_color = texture(skybox, text_coords);
    //frag_color = vec4(1.0, 1.0, 0.0, 1.0);
}
